{
  "name": "personality-pentagon",
  "version": "1.0.0",
  "description": "Berlin personality quiz with pentagon chart",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
